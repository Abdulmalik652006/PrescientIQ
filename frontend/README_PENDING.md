# Frontend — Coming in Phase 3

The React + Vite frontend (Dashboard, Analytics, Predict, Alerts, Resources,
Forecasts, Teams, Reports, Settings) has not been built yet.

It will be added here in the same structure as described in the project's
root README: src/components, src/pages, src/services, src/context, etc.,
wired to the backend API at VITE_API_URL (default http://localhost:5000/api).
